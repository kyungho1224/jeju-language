import 'package:flutter/material.dart';

class SubScreen4 extends StatelessWidget {
  const SubScreen4({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('sub 4'),
    );
  }
}
